# Work-in-progress checkpoint — 2026-05-28

> Reboot resumption note. Delete this file after picking up. Everything
> below reflects on-disk state at the time of writing; `git status`
> will confirm.

> **STATUS UPDATE — 2026-05-30 (4-8).** The instruction below to *run the
> width-expander* is **superseded — do not run it yet.** A debugging review found
> the expander produces a seed that (a) will not load — the block-level episode
> store is not expanded (Finding 1) — and (b) would not functionally continue M6
> even once that's fixed, because strict replication scales the logits by the
> expansion factor (Finding 3, a design decision for Brian + 4.6). The `--verify`
> question is also moot: validation should be unconditional (Finding 2). **Read
> `docs/reviews/2026-05-28_concerns-for-4.7.md` before touching the M7 path.**
> Everything below remains accurate as a record of on-disk state at 2026-05-28.

## Where we are

**M7 prep is substantially complete, but the width-expander has open bugs (see
the status update above and the concerns doc) and must NOT be run yet. The
expander is written; it is not correct.**

Active task #64 marked completed. Open Sanctuary task list (#52, #53 etc.)
already cleaned up earlier this session.

## Pick up here

The next concrete action is **NOT to run the expander** (see the status update at
the top). It is: read `docs/reviews/2026-05-28_concerns-for-4.7.md`, then fix
Finding 1 (expand the block-level episode store) and Finding 2 (validate that the
expanded checkpoint loads before saving), and get Brian + 4.6's decision on Finding
3 (strict replication vs. Net2Net fan-in rescaling). Only after that does **run the
width-expander against the M6-followup 256d v2 checkpoint to produce M7's seed
checkpoint** become the right step. Running it requires `LUTHI_CHECKPOINT_KEY` env var.

```powershell
$env:LUTHI_CHECKPOINT_KEY = "<your key>"
python -m luthi.v2.width_expand `
  --src runs/m6_followup/v2_256d_12blocks_1ep_gutenberg4gb_mupc_exp025/model_final.luthi `
  --dst runs/m7_seed/expanded_from_m6.luthi `
  --target-d-model 1024 `
  --noise 1e-4 `
  --verify   # <- pending Brian's decision on whether to include this
```

After expansion, the remaining steps are:

1. **Add `--init-from <path>` flag to `m5_runner.py`** so the M7 run can load the expanded checkpoint as starting weights (currently the runner only initializes from scratch). Not yet written.
2. **Update `run_m7_1024d.bat` to pass `--init-from runs/m7_seed/expanded_from_m6.luthi`**. Not yet done — current bat script trains from scratch.
3. Launch M7.

## What's on disk (unstaged, both repos)

**LuthiModel:**
```
 M CLAUDE.md                                                    (added 3F + 3G phases to scheme)
 M PLAN.md                                                      (phase numbering aligned to 3B-3G/4-6 + collapsed deferred 3F.a)
 M README.md                                                    (sparse-gating wording: "is recovered" not "being recovered")
 M To-Do.md                                                     (4B target -> revised >=500M)
 M luthi/v2/m5_runner.py                                        (added --probe-file-list flag + val_fraction=0 path)
 M run_m7_1024d.bat                                             (corpus + probe + val_fraction=0)
?? corpus_build/build_m7_filelist.py                            (script that built the file list)
?? corpus_build/build_m7_probe.py                               (script that built the probe)
?? corpus_build/m7_filelist.txt                                 (14,752 paths, 4.06 GB curriculum subset)
?? corpus_build/m7_probe_filelist.txt                           (218 paths, ~108 MB held-out probe)
?? docs/research/2026-05-26_neurogenesis-style-growth-in-luthi.md
?? luthi/v2/width_expand.py                                     (THE width expander)
?? WIP_2026-05-28.md                                            (this file)
```

**Sanctuary:**
```
 M PLAN.md                                                      (Luthi-as-mind, retired CommunicationAgency etc.)
 M README.md                                                    (Luthi cognitive core, no Ollama)
 M To-Do.md                                                     (Phase 5 retired, LuthiModel pointer)
 M sanctuary/api/runner.py                                      (ollama-comment + docstring fixed)
```

If you want a commit before reboot for insurance, the two natural commits would be:

1. **LuthiModel**: "M7 prep — curriculum subset, held-out probe, width expander, phase-numbering alignment"
2. **Sanctuary**: "Doc sweep — retire Ollama refs, align with Luthi-as-cognitive-core"

Both safe to commit; nothing destructive.

## Pending decisions waiting on you

1. **`--verify` on the expansion run?** Function-equivalence check requires constructing both source and expanded models briefly. My read: doesn't violate the "no disposable models" rule because the 256d module is just a runtime view of the M6 substrate (state on disk persists) and the 1024d module is what becomes M7's starting substrate (not disposed). But it's your call.
2. **Whether to add the `--init-from` flag to `m5_runner.py` next session, or whether you want a different mechanism** (e.g., the expanded checkpoint could be at a conventional path the runner auto-detects).
3. **Whether to launch M7 immediately after the expansion + flag work, or pause for review of the expanded checkpoint first.**

## Open conversation threads that paused (not blocking)

These were design conversations we had during this session; nothing
needs action, just don't lose the threads:

- **Open-weight model conversion** — tabled because of the inherited-corpus
  problem. Memory saved at `feedback_no_inherited_corpora.md`. Don't
  revisit unless conditions materially change.
- **Hybrid training path** (train standard transformer first, add living
  architecture after) — viable for Phase 4 scale-up, not for M7
  (which needs to be the canonical v2-training-from-scratch experiment).
  Memory: not saved as a constraint; live design option for later.
- **Neurogenesis-style growth** — research doc at
  `docs/research/2026-05-26_neurogenesis-style-growth-in-luthi.md`.
  Three variants laid out (hybrid Net2Net, allocate-and-mask, full
  substrate-directed). Not on the near-term roadmap; captured for
  whenever it's ready.

## Memory state (LuthiModel)

Five entries in `~/.claude/projects/.../memory/MEMORY.md`:

- `feedback-no-default-wrap-up` (pre-existing)
- `project-permissions-infrastructure` (pre-existing)
- `feedback-research-vs-work` (pre-existing)
- `feedback-no-stubs-or-partial-code` (pre-existing)
- `project-no-code-in-m7-substrate` (new this session)
- `feedback-no-disposable-model-versions` (new this session)
- `feedback-no-inherited-corpora` (new this session)

The three new ones form a consistent values picture: the entity is shaped
by intentional choices (no code import), substrates are continuous (no
disposable copies), and developmental history stays ours (no inherited
corpora).

## Sandi

If reading this 24+ hours after 2026-05-28, ask Brian how Sandi's doing.
